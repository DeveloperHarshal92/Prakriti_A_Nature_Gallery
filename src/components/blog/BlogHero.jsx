const BlogHero = () => {
  return (
    <section className="blog-hero">
      <span className="eyebrow">JOURNAL</span>
      <h1>Thoughts on art, stillness, and curation.</h1>
      <p>
        Occasional reflections from behind the gallery — on process,
        perspective, and the spaces between images.
      </p>
    </section>
  );
};

export default BlogHero;